/**
 * Legacy component placeholder.
 * The certification strip section was removed from the homepage
 * to reduce duplication with footer messaging and partner logos.
 *
 * This file is intentionally left without a component export so imports fail at build time.
 */

export {};
