# 📧 郵件系統架構圖

## 🏗️ 系統流程圖

```
┌─────────────────────────────────────────────────────────────────┐
│                        使用者瀏覽器                              │
│  ┌────────────────┐                    ┌─────────────────┐     │
│  │ Booking Form   │                    │ Contact Form    │     │
│  │ /booking       │                    │ /contact        │     │
│  └───────┬────────┘                    └────────┬────────┘     │
└──────────┼─────────────────────────────────────┼───────────────┘
           │ POST /api/booking                   │ POST /api/contact
           │ {name, email, phone, ...}           │ {name, email, message}
           │                                     │
           ▼                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js API Routes                         │
│  ┌───────────────────────┐       ┌──────────────────────┐      │
│  │ /api/booking/route.ts │       │ /api/contact/route.ts│      │
│  │                       │       │                       │      │
│  │ 1. 驗證表單數據         │       │ 1. 驗證表單數據        │      │
│  │ 2. 格式化郵件內容       │       │ 2. 格式化郵件內容      │      │
│  │ 3. 呼叫 email service  │       │ 3. 呼叫 email service │      │
│  └───────────┬───────────┘       └──────────┬───────────┘      │
└──────────────┼──────────────────────────────┼──────────────────┘
               │                              │
               └──────────────┬───────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Email Service Layer                          │
│                   src/lib/email.ts                              │
│  ┌─────────────────────────────────────────────────┐           │
│  │           Nodemailer Transporter                │           │
│  │                                                  │           │
│  │  Configuration:                                  │           │
│  │  - Service: Gmail                                │           │
│  │  - Auth: {                                       │           │
│  │      user: process.env.EMAIL_USER               │           │
│  │      pass: process.env.EMAIL_PASSWORD           │           │
│  │    }                                             │           │
│  └──────────────────────┬───────────────────────────┘           │
└─────────────────────────┼───────────────────────────────────────┘
                          │
                          │ SMTP Connection
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Gmail SMTP Server                          │
│                     smtp.gmail.com:587                          │
│                                                                  │
│  Authentication: App Password (16 digits)                       │
│  Security: TLS/STARTTLS                                         │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       │ Send Email
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      目標信箱                                     │
│                info@waymakerbiz.com                             │
│                                                                  │
│  📧 接收：                                                        │
│  - 課程預約通知（HTML 格式）                                       │
│  - 聯絡表單訊息（HTML 格式）                                       │
│  - 可直接點擊「回覆」聯絡客戶                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📂 檔案結構與職責

```
cpr/
│
├── .env.local                           # 🔐 環境變數（你需要填寫）
│   ├── EMAIL_USER=your-email@gmail.com
│   ├── EMAIL_PASSWORD=app-password
│   └── EMAIL_TO=info@waymakerbiz.com
│
├── src/
│   ├── lib/
│   │   └── email.ts                     # 📧 郵件服務層
│   │       ├── transporter              # Nodemailer 配置
│   │       └── verifyEmailConfig()      # 驗證連接
│   │
│   └── app/
│       ├── booking/
│       │   └── page.tsx                 # 📝 Booking 表單
│       │       └── handleSubmit()       # POST to /api/booking
│       │
│       ├── contact/
│       │   └── page.tsx                 # 📝 Contact 表單
│       │       └── handleSubmit()       # POST to /api/contact
│       │
│       └── api/
│           ├── booking/
│           │   └── route.ts             # 🔌 Booking API
│           │       ├── POST()
│           │       ├── 驗證數據
│           │       ├── 格式化 HTML/Text
│           │       └── 發送郵件
│           │
│           └── contact/
│               └── route.ts             # 🔌 Contact API
│                   ├── POST()
│                   ├── 驗證數據
│                   ├── 格式化 HTML/Text
│                   └── 發送郵件
│
├── scripts/
│   └── test-email.js                    # 🧪 測試腳本
│       └── 驗證 SMTP 配置和發送測試郵件
│
└── docs/
    ├── email-quick-setup.md             # 🚀 快速設定指南
    ├── email-setup-guide.md             # 📖 完整設定指南
    └── email-implementation-summary.md  # 📊 技術總結
```

---

## 🔄 數據流詳解

### 1️⃣ 用戶提交表單

```typescript
// src/app/booking/page.tsx
const handleSubmit = async (event) => {
  // 前端驗證
  const errors = validate();
  if (errors) return;

  // 準備數據
  const bookingData = {
    fullName: values.name,
    phone: values.phone,
    email: values.email,
    // ... 其他欄位
    locale: "en" | "zh"
  };

  // 發送到 API
  const response = await fetch("/api/booking", {
    method: "POST",
    body: JSON.stringify(bookingData)
  });
};
```

### 2️⃣ API 處理請求

```typescript
// src/app/api/booking/route.ts
export async function POST(request) {
  // 解析請求
  const body = await request.json();

  // 後端驗證
  if (!body.fullName || !body.email) {
    return NextResponse.json({ error: "Missing fields" }, { status: 400 });
  }

  // 準備郵件內容
  const htmlContent = `<html>...</html>`;  // 美化的 HTML
  const textContent = `...`;                // 純文字備份

  // 發送郵件
  await transporter.sendMail({
    from: EMAIL_USER,
    to: EMAIL_TO,
    replyTo: body.email,  // 可直接回覆客戶
    subject: "新課程預約",
    html: htmlContent,
    text: textContent
  });

  return NextResponse.json({ success: true });
}
```

### 3️⃣ 郵件服務發送

```typescript
// src/lib/email.ts
import nodemailer from "nodemailer";

export const transporter = nodemailer.createTransport({
  service: "gmail",
  auth: {
    user: process.env.EMAIL_USER,      // your-email@gmail.com
    pass: process.env.EMAIL_PASSWORD   // App Password
  }
});

// 驗證配置
export async function verifyEmailConfig() {
  await transporter.verify();
  console.log("✅ Ready to send emails");
}
```

---

## 🎨 郵件模板結構

### HTML 郵件模板

```html
<!DOCTYPE html>
<html>
  <head>
    <style>
      /* 品牌色彩 */
      .header { background: #2F4858; color: white; }
      .content { background: #f9f9f9; padding: 30px; }
      .label { font-weight: bold; color: #2F4858; }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- 標題區 -->
      <div class="header">
        <h1>🚨 新的課程預約</h1>
        <p>New Course Booking</p>
      </div>

      <!-- 內容區 -->
      <div class="content">
        <div class="field">
          <span class="label">姓名 / Full Name:</span>
          <span class="value">${fullName}</span>
        </div>
        <!-- ... 其他欄位 -->
      </div>

      <!-- 頁腳 -->
      <div class="footer">
        <p>此郵件由 Waymaker CPR 網站自動發送</p>
      </div>
    </div>
  </body>
</html>
```

---

## 🔐 安全性架構

```
┌─────────────────────────────────────────────────────────┐
│                    安全性層次                            │
└─────────────────────────────────────────────────────────┘

1. 環境變數保護
   ├── .env.local (本地開發)
   │   └── ❌ 永遠不提交到 Git (.gitignore)
   └── Vercel Environment Variables (生產環境)
       └── ✅ 加密儲存在 Vercel

2. Gmail App Password
   ├── ✅ 只能發送郵件（無法登入帳號）
   ├── ✅ 可隨時撤銷
   └── ✅ 與主密碼分離

3. API Route 保護
   ├── ✅ 後端驗證（不信任前端）
   ├── ✅ 錯誤處理（不洩露敏感資訊）
   └── ✅ Rate limiting（可選，防止濫用）

4. SMTP 連接
   ├── ✅ TLS/STARTTLS 加密
   └── ✅ Gmail 的安全基礎設施
```

---

## 📊 郵件發送限制

### Gmail 免費帳號

```
┌────────────────────────────────────────┐
│  Gmail Free Account Limits             │
├────────────────────────────────────────┤
│  每天最多：500 封                       │
│  每分鐘約：10 封                        │
│  附件大小：25 MB                        │
└────────────────────────────────────────┘

適合情境：
✅ 小型網站
✅ 每天少於 50 個提交
✅ 測試和開發

不適合：
❌ 大型網站
❌ 高流量應用
❌ 需要發送確認郵件給客戶
```

### 升級選項

如果需要更多容量：

```
┌──────────────────────┬─────────┬──────────────────┐
│ 服務                 │ 費用     │ 每月免費額度      │
├──────────────────────┼─────────┼──────────────────┤
│ Google Workspace     │ $6/月   │ 每天 2,000 封    │
│ Resend              │ $0-20   │ 3,000 封         │
│ SendGrid            │ $0-20   │ 100 封/天        │
│ AWS SES             │ 按用量   │ 62,000 封/月     │
└──────────────────────┴─────────┴──────────────────┘
```

---

## 🧪 測試流程

```
┌─────────────────────────────────────────────────────────┐
│                  測試流程圖                              │
└─────────────────────────────────────────────────────────┘

1. 環境配置測試
   $ node scripts/test-email.js
   ├── ✅ 檢查環境變數
   ├── ✅ 驗證 SMTP 連接
   └── ✅ 發送測試郵件

2. Booking 表單測試
   ├── 訪問 /booking
   ├── 填寫所有必填欄位
   ├── 提交表單
   ├── 檢查前端成功訊息
   ├── 檢查 Terminal 無錯誤
   └── 檢查信箱收到郵件

3. Contact 表單測試
   ├── 訪問 /contact
   ├── 填寫所有必填欄位
   ├── 提交表單
   ├── 檢查前端成功訊息
   ├── 檢查 Terminal 無錯誤
   └── 檢查信箱收到郵件

4. 錯誤處理測試
   ├── 測試空欄位提交
   ├── 測試無效 email 格式
   ├── 測試網路錯誤
   └── 確認友善錯誤訊息
```

---

## 🎯 完整功能清單

### ✅ 已實作功能

- [x] Gmail SMTP 整合
- [x] Booking 表單郵件
- [x] Contact 表單郵件
- [x] HTML 郵件模板
- [x] 純文字備份
- [x] 雙語支援（中英文）
- [x] Reply-To 功能
- [x] 錯誤處理
- [x] 前端驗證
- [x] 後端驗證
- [x] 測試腳本
- [x] 完整文檔

### 📋 可選增強功能

- [ ] 自動回覆郵件給客戶
- [ ] 郵件模板引擎（React Email）
- [ ] 郵件記錄到資料庫
- [ ] 管理後台查看提交
- [ ] 郵件佇列系統
- [ ] Rate limiting
- [ ] 垃圾郵件防護（reCAPTCHA）
- [ ] 分析和報表

---

## 📚 相關資源

### 文檔
- [快速設定](email-quick-setup.md) - 5 分鐘快速上手
- [完整指南](email-setup-guide.md) - 詳細設置步驟
- [技術總結](email-implementation-summary.md) - 架構和 API 細節

### 外部資源
- [Nodemailer 文檔](https://nodemailer.com/)
- [Gmail App Passwords](https://support.google.com/accounts/answer/185833)
- [Next.js API Routes](https://nextjs.org/docs/app/building-your-application/routing/route-handlers)
